// 
console.log ( null > 0);
console.log ( null == 0);
console.log ( null >= 0 ) ; // here the null get converted into 0 
// undefined gives false value applied in all these three operations 

console.log ( "2" === 2 ) ; // now it does not give the same value as before nnow  will check its datatype also